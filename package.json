{
  "name": "ofek-website",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/check-i18n.mjs && node scripts/check-css.mjs",
    "build:fast": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:i18n": "node scripts/check-i18n.mjs",
    "check:css": "node scripts/check-css.mjs",
    "astro": "astro",
    "visual-diff": "node scripts/visual-diff.mjs"
  },
  "dependencies": {
    "astro": "^7.1.6",
    "motion": "^12.43.0",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "playwright": "^1.62.1",
    "typescript": "^5.9.2"
  }
}
